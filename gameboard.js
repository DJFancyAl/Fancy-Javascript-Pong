let gameboard = {
    board: document.querySelector('canvas'),
    ctx: this.board.getContext('2d')
    // clear: function(){
    //     let ctx = gameboard.board.getContext('2d')
    //     ctx.clearRect(0, 0, board.width, board.height)
    // }
}
